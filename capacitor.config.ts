import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'it.readeal.app',
  appName: 'ReaDeal',
  webDir: 'www',
  server: {
    "cleartext": true
  }
};

export default config;
